{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter=@linguoup/database build && pnpm --filter=web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile"
}
