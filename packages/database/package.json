{
  "name": "@linguoup/database",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "seed": "ts-node prisma/seed.ts",
    "studio": "prisma studio"
  },
  "devDependencies": {
    "@linguoup/config": "workspace:*",
    "eslint": "^9.18.0"
  }
}
